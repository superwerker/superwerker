name: Automated Acceptance Tests

on:
  workflow_dispatch: 
    inputs:
      account:
        required: true
        default: 'pipeline01'
        type: choice
        description: 'The account to deploy to'
        options:
          - pipeline01
          - pipeline02

permissions:
  id-token: write
  contents: write

env:
  AWS_DEFAULT_REGION: eu-central-1

jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ inputs.account }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
          role-to-assume: ${{ secrets.AWS_RELEASE_ROLE }}

      - name: Build Superwerker
        run: |
          yarn global add aws-cdk
          yarn install
          yarn test
          yarn synth
          yarn publish-assets
        env:
          SUPERWERKER_VERSION: 0.0.0-AAC
          NODE_ENV: development
        shell: bash
        working-directory: cdk

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
          role-to-assume: ${{ secrets.AWS_AAC_ROLE }}


      - name: echo
        run: |
          echo $SUPERWERKER_DOMAIN >> secrets.txt
        env:
          SUPERWERKER_DOMAIN: ${{ secrets.SUPERWERKER_DOMAIN }}


      - name: Run tmate
        uses: mxschmitt/action-tmate@v2

      - name: Deploy Superwerker
        run: |
          
          aws cloudformation deploy --stack-name superwerker --template-file ./cdk.out/SuperwerkerStack.template.json --parameter ParameterKey=Domain,ParameterValue=${SUPERWERKER_DOMAIN} ParameterKey=Subdomain,ParameterValue=${{ inputs.account }} ParameterKey=NotificationsMail,ParameterValue=root+notifications@${{ inputs.account }}.${SUPERWERKER_DOMAIN} --capabilities CAPABILITY_AUTO_EXPAND CAPABILITY_IAM CAPABILITY_NAMED_IAM --disable-rollback  --no-cli-pager
        env:
          SUPERWERKER_VERSION: 0.0.0-AAC
          SUPERWERKER_DOMAIN: ${{ secrets.SUPERWERKER_DOMAIN }}
          TT: ${{ vars.TT }}
        shell: bash
        working-directory: cdk
